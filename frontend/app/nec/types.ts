export type NoteType = {
  _id: string;
  name: string;
  content: string;
  subject: string;
  created: string;
};

export type SubjectType = {
  _id: string;
  name: string;
  createdAt: string;
};
